<%# <h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p> %>

<h1>Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list.id) %><br>
  <% end %>
</ul>
<%= link_to "add a new list", new_list_path %>
